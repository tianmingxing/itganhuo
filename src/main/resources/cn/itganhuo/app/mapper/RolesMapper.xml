<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.itganhuo.app.dao.RolesDao">

	<cache/>
	
	<sql id="roles_column_list">
		ID, ROLE, DESCRIPTION, ISAVAILABLE
	</sql>

	<resultMap id="roles_result" type="Roles">
		<id column="id" property="ID" jdbcType="VARCHAR" />
		<result column="role" property="ROLE" jdbcType="VARCHAR" />
		<result column="description" property="DESCRIPTION" jdbcType="VARCHAR" />
		<result column="isAvailable" property="ISAVAILABLE" jdbcType="TINYINT" />
	</resultMap>
	
	<select id="loadById" resultMap="roles_result" parameterType="java.lang.String">
		SELECT
		<include refid="roles_column_list" />
		FROM T_ROLES
		WHERE ID = #{id}
	</select>
	
	<select id="getRolesList" resultMap="roles_result" parameterType="Map">
		SELECT
		<include refid="roles_column_list" />
		FROM T_ROLES
		<choose>
			<when test="paging != null and paging.sort != null and paging.order != null">
				ORDER BY ${paging.sort} ${paging.order}
			</when>
			<otherwise>
				ORDER BY ID DESC
			</otherwise>
		</choose>
		<if test="paging.page > 0 and paging.rows > 0">
			LIMIT #{paging.offrow}, #{paging.rows}
		</if>
	</select>
	
	<select id="countRolesList" resultType="int" parameterType="Map">
		SELECT
		COUNT(1)
		FROM T_ROLES
	</select>

	<insert id="insert" parameterType="Roles" useGeneratedKeys="true" keyProperty="ID">
		INSERT INTO T_ROLES
		<trim prefix="(" suffix=")" suffixOverrides=",">
			<if test="id != null">ID,</if>
			<if test="role != null">ROLE,</if>
			<if test="description != null">DESCRIPTION,</if>
			<if test="isAvailable != null">ISAVAILABLE,</if>
		</trim>
		<trim prefix="values (" suffix=")" suffixOverrides=",">
			<if test="id != null">#{id},</if>
			<if test="role != null">#{role},</if>
			<if test="description != null">#{description},</if>
			<if test="isAvailable != null">#{isAvailable},</if>
		</trim>
	</insert>
	
	<update id="updateInfo" parameterType="Roles">
		UPDATE T_ROLES
		<set>
			<if test="role != null">ROLE = #{role},</if>
			<if test="description != null">DESCRIPTION = #{description},</if>
			<if test="isAvailable != null">ISAVAILABLE = #{isAvailable}</if>
		</set>
		WHERE ID = #{id}
	</update>
	
	<delete id="delete" parameterType="java.lang.String">
		DELETE FROM T_ROLES WHERE ID = #{id}
	</delete>
</mapper>